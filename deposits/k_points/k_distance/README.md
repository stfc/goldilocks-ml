# k-distance models

Models predicting the largest spacing between adjacent k-points, in Å⁻¹.
Goldilocks Core converts that value to an integer mesh using the reciprocal
lattice. Each model has its own directory holding a model card, PSDI metadata,
and an artifact manifest.
